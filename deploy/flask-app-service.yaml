apiVersion: v1
kind: Service
metadata:
  name: flask-demo-app-service
  namespace: flask-app
spec:
  type: LoadBalancer
  selector:
    components: flask-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
